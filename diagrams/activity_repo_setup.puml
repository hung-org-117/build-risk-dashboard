@startuml
title Repository Setup and Synchronization

|Admin|
start
:Open repository management;
:Select connect/import repository;

|System|
:Display available repositories;

|Admin|
:Choose repository and configure settings;

|System|
:Validate access and configuration;
if (Access valid?) then (yes)
  :Save repository configuration;
  :Queue synchronization job;

  |Github|
  :Return repository metadata and workflow runs;

  |System|
  :Ingest builds and store raw records;
  :Trigger feature extraction pipeline;
  :Update repository status;

  |Admin|
  :View repository and build list;
  stop
else (no)
  :Notify access/validation error;
  |Admin|
  :Revise selection or credentials;
  stop
endif
@enduml
