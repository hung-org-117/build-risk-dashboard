<h2>🔴 Pipeline Failed</h2>

<div class="alert-critical">
    <strong>Feature extraction pipeline failed for {{repo_name}}</strong>
</div>

<div class="stats-grid">
    <div class="stat-item">
        <div class="stat-value">{{repo_name}}</div>
        <div class="stat-label">Repository</div>
    </div>
</div>

<h3>Error Details</h3>
<div
    style="background: #fee; border-radius: 6px; padding: 16px; margin: 16px 0; font-family: monospace; font-size: 13px; white-space: pre-wrap; word-break: break-word;">
    {{error_message}}</div>

<h3>💡 Recommended Actions</h3>
<ul>
    <li>Check the repository logs for more details</li>
    <li>Review recent changes to the pipeline configuration</li>
    <li>Retry the failed processing if it was a transient error</li>
</ul>

<p style="text-align: center; margin-top: 20px;">
    <a href="{{app_url}}/repositories" class="button">View Repositories</a>
</p>